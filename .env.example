# Database Configuration
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Server Configuration
PORT=5001
NODE_ENV=development

# Email Configuration (using Resend)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here










# Optional: Set a custom from address (requires domain verification in Resend)
# EMAIL_FROM=TalentFlux <hello@yourdomain.com>

# Application URL (used in emails)
APP_URL=http://localhost:5001

# OpenAI Configuration (for CV generation)
OPENAI_API_KEY=your-openai-api-key

# Session Secret (generate a random string)
SESSION_SECRET=your-random-session-secret
# Default: onboarding@resend.dev (for testing)
